[project]
name = "hackathon-app"
version = "0.1.0"
description = "A hackathon project"
requires-python = ">=3.9"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.16.0",
    "openapi>=2.0.0",
    "pandas>=2.3.3",
]

[tool.uv]
# UV configuration
